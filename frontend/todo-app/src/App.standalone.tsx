/**
 * Standalone App component (without backend integration)
 * For use in development and testing of UI components only
 * This is the version built for Ticket #3
 */

import { TodoApp } from './TodoApp';

function App() {
  return <TodoApp />;
}

export default App;
